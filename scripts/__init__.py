"""s2c utility scripts.

Run any of the modules directly with `python scripts/<name>.py` from the
repo root. ``scripts/`` is not on the install path — these are dev-time
ops tools, not user-facing.
"""
